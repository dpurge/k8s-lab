---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: adminer
  namespace: data
spec:
  replicas: 1
  selector:
    matchLabels:
      app: adminer
  template:
    metadata:
      labels:
        app: adminer
    spec:
      containers:
        - name: adminer
          image: adminer:5.5.1-standalone
          ports:
            - containerPort: 8080
          env:
            # Prefills the Adminer login form's server field
            - name: ADMINER_DEFAULT_SERVER
              value: postgres.data.svc.cluster.local
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: adminer
  namespace: data
spec:
  selector:
    app: adminer
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: adminer
  namespace: data
spec:
  ingressClassName: traefik
  rules:
    - host: adminer.localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: adminer
                port:
                  number: 8080
