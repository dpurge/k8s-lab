# Build artifacts — excluded to prevent cache poisoning; cargo build runs inside the container
target/

# Git metadata — not needed in the build context
.git/

# Kubernetes manifests and project docs — build-time irrelevant
k8s/

# Markdown and misc project files
*.md

# k3d-generated credentials and config
.k3d/
