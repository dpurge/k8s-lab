# Project Memory

<!-- Append-only, one entry per line: `YYYY-MM-DDTHH:MM:SSZ [tag] text`. See the `memory-format` skill. Do not read this file in full — grep it. -->

2026-09-21T16:13:31Z [gotcha] Ollama model llama3-chatqa:8b has capabilities ["completion"] only (GET /api/show); a non-empty "tools" field in POST /api/chat returns HTTP 400 "does not support tools".
2026-09-21T16:13:32Z [gotcha] Ollama model llama3-chatqa:8b refuses an id=/score=/title= metadata-list prompt; plain concatenated text works (knowledge/internal/chat/chat.go answer()).
2026-09-21T16:13:33Z [gotcha] Ollama model llama3-chatqa:8b answers tersely (sometimes one word) unless the question ends with "Please give a full and complete answer."
2026-09-21T16:13:34Z [env] Ollama's POST /api/chat uses a 4096-token default context regardless of model max, unless the body sets options.num_ctx (shared/llm.Config.NumCtx).
2026-09-21T17:18:33Z [gotcha] Ollama model gemma4:12b has a "thinking" capability on by default; without think:false in POST /api/chat, one request took 148s and still answered wrong.
2026-09-21T17:18:34Z [gotcha] Sending "think": false in Ollama's POST /api/chat body disables the hidden reasoning trace; the same 148s wrong-answer request dropped to 14s and correct.
2026-09-21T17:18:35Z [gotcha] Ollama tags gemma4:e4b and gemma4:e2b are not small despite the name: model layer downloads are 9.6GB and 7.2GB, bigger than gemma4:12b's ~8GB.
2026-09-21T17:18:36Z [decision] knowledge app's chat.model/generate.model (config.yaml) default to gemma4:12b; llama3-chatqa:8b hallucinates on general instruction tasks like title/summary generation.
2026-09-21T17:30:36Z [env] On a 16GB dev machine, overlapping concurrent Ollama requests can starve the k3d node until kubelet marks it NotReady; issue LLM calls sequentially.
2026-09-21T17:55:51Z [domain] For this knowledge collection with bge-m3 embeddings, genuinely relevant Qdrant matches scored 0.51-0.76 and irrelevant ones scored 0.19-0.34 across several queries.
2026-09-21T17:55:52Z [decision] qdrant.Client filters results below qdrant.searchMinScore (config.yaml, default 0.4, 0 disables) in vectorSearch(); scroll()/tag-only listing is unaffected.
2026-09-21T18:39:17Z [convention] knowledge app config (models/endpoints/thresholds) comes from a mounted config.yaml (CONFIG_FILE); PGUser/PGPassword/*APIKey stay env vars, never in that file.
2026-09-21T18:39:18Z [gotcha] Taskfile.yml's deploy-knowledge runs the migrate Job before applying deployment.yaml; a resource the Job needs (e.g. a ConfigMap) must be applied earlier in that task.
2026-09-21T18:51:20Z [domain] Ollama model rinex20/translategemma3:12b has capabilities ["completion"] only (no thinking); one instruction passes through target-language text and translates otherwise.
2026-09-21T19:03:42Z [convention] knowledge/internal/jobs.Service tracks async work generically (Start/SetStep/Finish/Current); GET /api/v1/jobs/current is polled by the header status line.
2026-09-21T19:03:43Z [gotcha] knowledge/README.md's architecture tree missed internal/translate and internal/jobs when those packages were added; each was only caught in a later spec's own doc review.
2026-09-21T20:21:19Z [gotcha] Go's regexp (RE2) engine has no backreferences; a pattern matching multiple alternative closing tags (e.g. </script> or </style>) must spell out each alternative explicitly rather than capturing and backreferencing a tag name. See knowledge/internal/ingest/html.go.
2026-09-21T20:21:19Z [convention] Postgres-row-backed structs in the knowledge app use snake_case JSON tags matching column names (e.g. created_at, job_id), not camelCase. See knowledge/internal/qdrant/qdrant.go's Item and knowledge/internal/ingest/drafts.go's Draft.
2026-09-21T20:21:19Z [gotcha] Go's net/http test server auto-sniffs and sets a Content-Type header on the first ResponseWriter.Write call when the handler set none; a test asserting "missing Content-Type" behavior must explicitly set the header to an empty string to suppress sniffing.
2026-09-21T20:21:19Z [decision] A Postgres partial unique index (CREATE UNIQUE INDEX ... WHERE status='running') closes a check-then-insert TOCTOU race for a single-concurrent-job guard; pgx v5 surfaces the conflict as *pgconn.PgError with Code "23505" (unique_violation), translatable via errors.As. See knowledge/internal/jobs/jobs.go's Start and jobs_one_running_idx.
2026-09-21T20:21:19Z [gotcha] A goroutine that reports its own outcome via a context-scoped call (e.g. jobs.Finish) must use a context derived fresh from context.Background(), not the goroutine's own possibly-just-expired timeout context, or a timed-out run can never record its own failure. See knowledge/internal/ingest/ingest.go's finish helper.
2026-09-21T21:32:12Z [gotcha] In this frontend, document.activeElement.closest(selector) is unreliable for detecting which tab is visually active: non-focusable elements (a clickable <div>) never change activeElement on click, and whether a clicked <button> gets focus at all is browser-dependent (Safari/Firefox on macOS vs Chrome). Use document.querySelector(".tab.active") instead. See knowledge/internal/server/static/index.html's run().
2026-09-21T21:32:12Z [decision] A DELETE ... WHERE id=$1 RETURNING inside a Postgres transaction closes a claim-then-act race without an explicit lock: the row lock from DELETE serializes concurrent claimants, and a second claimant simply gets zero rows (not-found) once the first commits. Distinct from a partial unique index (used elsewhere for a different race) — this technique fits a one-shot "claim and consume" flow. See knowledge/internal/ingest/drafts.go's claimDraft.
2026-09-21T21:32:12Z [convention] To cross a package import-graph invariant (e.g. "ingest never imports qdrant") for exactly one sanctioned operation, define a small consumer-side interface taking only primitives, satisfied by an adapter built where both types are already imported (main.go). See ingest.Promoter and main.go's qdrantPromoter.
2026-09-21T21:32:12Z [gotcha] This codebase had no prior Postgres transaction usage before knowledge-ingest-ui; pgxpool.Pool.Begin(ctx) returns (pgx.Tx, error) (pgx v5.10.0), and tx.Rollback(ctx) after a successful tx.Commit(ctx) is a documented no-op (returns pgx.ErrTxClosed, safe to ignore via defer).
2026-09-22T23:04:00Z [decision] Synchronous, request-blocking LLM/embedding calls (chat, generate, translate, ingest's per-chunk pipeline, qdrant.Create/Update/Import's upsert) should move toward the existing async job pattern (jobs.Service) rather than growing more inline blocking calls, because interactive/concurrent Ollama load is what starves the k3d node (see the 2026-09-21T17:30:36Z entry) — every new synchronous LLM-calling code path is a fresh way to trigger that, not just a fresh instance of the same known bug.
2026-09-22T23:04:01Z [env] On macOS, Ollama's model-serving subprocess (named llama-server, a child of the ollama serve process) can hold several GB of resident memory for one loaded model; on a resource-constrained dev machine, killing both processes is a fast way to recover a k3d node stuck NotReady from CPU/memory contention, verified live in this project.
2026-09-22T15:49:24Z [decision] knowledge/k8s/configmap.yaml's translate.model changed from rinex20/translategemma3:12b to gemma4:12b: a live French/German/Japanese/Spanish comparison via Ollama's /api/chat (with think:false) showed comparable translation quality, so the app now uses two Ollama models (bge-m3, gemma4:12b) instead of three.
2026-09-22T15:49:25Z [convention] knowledge/k8s/configmap.yaml's prompts: block (chat/generateTitle/generateSummary/translate) uses {{varname}} tokens for dynamic values (e.g. {{language}}), never Go fmt %s verbs; knowledge/internal/translate substitutes via strings.ReplaceAll so a ConfigMap editor need not know Go syntax.
2026-09-22T16:15:16Z [convention] A new file placed anywhere under knowledge/internal/server/static/ (any depth, e.g. a new subdirectory) is automatically embedded and served with no Go code change: server.go's //go:embed static/* plus its catch-all r.Handle("/*", http.FileServer(...)) already cover it; verified live for knowledge/internal/server/static/vendor/.
2026-09-22T16:15:17Z [decision] knowledge chat's Markdown rendering (knowledge/internal/server/static/index.html's md()) uses vendored (not CDN) markdown-it 15.0.2 + DOMPurify 3.4.15; markdown-it defaults to html:false and rejects javascript: links even without DOMPurify, verified live. <img> is allowed (DOMPurify default profile) per explicit user decision, accepting a tracking-pixel/IP-leak risk for attacker-controlled image markdown.
2026-09-22T17:12:26Z [convention] knowledge's kb-* Web Components (knowledge/internal/server/static/components/<tag>/) validated live: component.js registers a light-DOM custom element, injects component.css via a self-appended <link>, fetches+caches component.html via fetch() (mirrors phraseforge's ime.js pfLoadIme pattern), and ships a component.test.js run under Node+jsdom (task test-knowledge-frontend).
2026-09-23T14:29:05Z [gotcha] Appending a DocumentFragment via appendChild empties it; a component whose property setter stores then appends a caller-supplied DocumentFragment must cloneNode(true) it if the setter can be called more than once with the same object. See knowledge/internal/server/static/components/kb-message/component.js.
2026-09-23T14:29:06Z [convention] kb-nav's active pill state is read from its active attribute in _updateActiveState(), not clobbered by reassigning its .items property — so live-updating pill labels via .items = [...] is safe and preserves whichever pill is currently active. See knowledge/internal/server/static/components/kb-nav/component.js.
2026-09-23T14:29:07Z [gotcha] Node's node --test only discovers test files under its invocation working directory and subdirectories; a test file placed outside knowledge/internal/server/static/components/ (the directory task test-knowledge-frontend runs npm test from) is silently never run by that command.
2026-09-23T14:29:08Z [decision] The vendored DOMPurify build at knowledge/internal/server/static/vendor/purify.min.js supports the RETURN_DOM_FRAGMENT: true sanitize option, returning a sanitized DocumentFragment instead of an HTML string — used by kb-message's rendering pipeline to avoid ever re-parsing sanitized output via innerHTML.
2026-09-22T17:39:25Z [gotcha] A light-DOM custom element's connectedCallback cannot assume its own light-DOM children (text written between its tags in static HTML) already exist: for an element present in the initial document, the browser inserts the opening tag (firing connectedCallback) before parsing/appending the content between the tags. A queueMicrotask-deferred wrap (assuming children land within one microtask) passed a jsdom repro but was confirmed live, via real Chrome DevTools, to still fail in production. A MutationObserver({childList:true}) on the element, wrapping once a child actually appears, fixed it — verified live. See knowledge/internal/server/static/components/kb-button/component.js.
2026-09-22T18:50:40Z [gotcha] jsdom's custom-element + static-HTML-parsing simulation is not fully reliable even for a simple connectedCallback mutation (insertBefore of a brand-new node, not moving/depending on existing children): a real run showed the inserted node present immediately after connectedCallback, then silently gone by the time parsing finished, for reasons not reproduced outside jsdom. Given kb-button's own real-Chrome-vs-jsdom mismatch on this session, treat any kb-* jsdom "parse order" test as a smoke check, not proof of real-browser correctness; a MutationObserver-based self-repair (re-insert if the node is ever missing) made the jsdom test pass and costs nothing if real browsers never actually need it. See knowledge/internal/server/static/components/kb-field/component.js.
2026-09-22T19:15:09Z [gotcha] knowledge/internal/server/server.go's requireAuth-gated catch-all ("/*") does not automatically cover new static files a *public* page (login.html/signup.html) starts depending on — adding theme.css/auth.css/auth.js and the kb-button/kb-field components to those pages required adding matching public exact-path routes (and a serveStaticGlob helper for whole component directories) alongside the existing login.html/signup.html/theme.js allowlist in server.go, or an unauthenticated visitor gets a completely unstyled, non-functional login page. Check this allowlist whenever a public page's static dependencies change.
2026-09-22T19:15:10Z [convention] knowledge's theme.css defines only the :root/:root[data-theme=dark] CSS variable palette (shared by index.html/login.html/signup.html) — it does not apply any of them. Each page's own stylesheet (app.css, auth.css) must itself set body{background:var(--bg);color:var(--fg);font-family:...} to actually consume the palette; omitting that (as happened once, silently, when the combined stylesheet was first split into these files) leaves the page's own layout unthemed while kb-* components — which reference the variables directly in their own component.css — still look correctly themed, a misleading half-working symptom.
2026-09-22T17:12:27Z [gotcha] A CSS Grid item's default min-height:auto lets its content grow past a 1fr row's intended size instead of respecting it (breaks a pinned header/footer layout); set min-height:0 on the grid item to make the track size actually constrain it and let the item's own overflow:auto scroll instead. See knowledge/internal/server/static/index.html's .tab rule.
2026-09-22T21:07:29Z [gotcha] A queue table's single-running unique index (e.g. operations_one_running_idx) has no built-in recovery from a process restart: a claimed row stuck at status='running' when the process dies permanently blocks every future claim, since the index allows zero further running rows. A FailStale-style sweep (mark any 'running' row 'failed' at startup, before the worker starts) is required, mirroring jobs.Service.FailStale — confirmed live: a transient dependency outage caused a few crash-loop restarts, which orphaned a row and jammed knowledge/internal/queue's worker until FailStale was added.
2026-09-22T21:07:30Z [decision] A queue.Service handler invoked by its own worker (e.g. an ingest_chunk operation) must never call another method that itself enqueues-and-awaits on the same queue — with one worker, that second operation could never be claimed until the first (still-running) call returns, deadlocking forever. knowledge/internal/generate's TitleDirect/SummaryDirect (bypass the queue) exist specifically for callers already running inside a queue-claimed handler; Title/Summary (queued, awaited) are only for callers outside the worker (HTTP handlers).
2026-09-22T21:07:31Z [convention] knowledge/internal/ingest's URL source acquisition is its own queue operation kind (ingest_acquire) separate from chunking (ingest_chunk), not folded into start(): a single ingest_chunk-only design would force the URL fetch to happen synchronously before the first operation could be enqueued, reintroducing the blocking-the-HTTP-caller problem the queue exists to avoid.
2026-09-24T11:12:14Z [decision] Per the user, the header-menu/aside-navbar work planned for phraseforge-pf-components-foundation, knowledge-theme-parity-phraseforge, and knowledge-aside-collapse (specs/artifacts/phraseforge/roadmap.md, specs/artifacts/knowledge/roadmap.md) should be designed extensibly for a future shared cross-app nav/layout system covering phraseforge, knowledge, and any later app — not as app-specific one-offs — even though no roadmap item yet tracks that unification itself.
2026-09-24T11:43:26Z [gotcha] Converting a plain <button onclick="fn(...,this)"> to a kb-button/pf-button-style wrapping custom element changes what `this` means inside that onclick attribute: it is now the custom element itself, not the real inner <button> the handler expects. A handler that does `button.disabled=true`/`button.textContent=...` on that argument silently no-ops (property set on the wrapper has no effect) or, for textContent, destroys the real button entirely. Fix call sites to pass `this.querySelector('button')` instead — the real button is guaranteed to exist by the time a user can click it. See phraseforge's phraseforgeGenerate(...) call sites in new.html/edit.html/dialogs-new.html/dialogs-edit.html/vocab-edit.html/models-edit.html.
2026-09-24T11:43:27Z [convention] kb-button/pf-button only forward disabled/type/aria-label to their wrapped inner <button> — never arbitrary attributes or an inline style. A page-specific visual override that used to be an inline style (e.g. width:100% on a single full-width submit button) needs a narrowly-scoped CSS class instead (e.g. `pf-button.full-width`), added to the page's own stylesheet, not to the component's shared CSS. See phraseforge/internal/server/templates/layout.html's `.full-width` rule, used by login.html/signup.html.
2026-09-24T12:06:59Z [convention] Replacing a synchronous `onsubmit="return confirm(...)"` with an async in-app dialog (kb-dialog/pf-dialog's `.confirm()` returns a Promise) requires restructuring the form: mark it `data-confirm="message"` (no onsubmit), intercept its real `submit` event with `event.preventDefault()`, `await dialog.confirm(...)`, then call the form's native `.submit()` — not `.requestSubmit()`, which re-dispatches a `submit` event and would re-trigger the same listener (needing a re-entrancy guard `.submit()` doesn't). See phraseforge/internal/server/templates/layout.html's phraseforgeWireConfirmForms().
2026-09-24T12:07:00Z [gotcha] A jsdom test that executes real source code inside a `new JSDOM(html, {runScripts:"dangerously"})` sandbox (to test against the actual deployed script, not a hand-copied approximation) produces object literals that live in that sandbox's own vm realm: `assert.deepEqual`/`deepStrictEqual` on a whole object built partly inside that realm can fail a reference/prototype check even when every field matches. Compare individual primitive fields instead. See phraseforge/internal/server/static/components/layout-confirm-forms.test.js.
2026-09-24T13:02:14Z [gotcha] A classic (non-module, non-defer) <script src> executes the instant the HTML parser reaches its tag, not after the whole document is parsed — a page-shell script whose top-level code does document.getElementById for an element defined LATER in the same document (e.g. layout.html's <footer><pf-status-bar id="statusBar"> comes after <main class="content">, where texts-app.html's script tag sits) gets null and throws immediately. Every jsdom-based validation of this class of script missed it because each built its own synthetic HTML with every referenced element already present before running any script, which accidentally guarantees an ordering the real document does not. Fix: add the `defer` attribute to that script tag (executes after full parse, still before DOMContentLoaded, regardless of the tag's position). See phraseforge/internal/server/templates/texts-app.html.
2026-09-24T13:02:15Z [convention] Any future phraseforge SPA-shell page script (dialogs-app.js, vocab-app.js, models-app.js, admin-app.js, profile-app.js — see specs/artifacts/phraseforge/roadmap.md) that reads #statusBar (or anything else layout.html defines after <main class="content">) at its own top level must load via `<script defer src="...">`, not a plain `<script src="...">` — see the entry above for why.
2026-09-24T13:43:34Z [gotcha] layout.html's sidebar #language-filter select navigates on change via `window.location.href = listPath + '?language=' + value` (phraseforgeSetLanguageFilter) — a real page reload carrying the filter as a query param. The first two SPA shells (texts-app.js, dialogs-app.js) initially never read that query param in their own showList()/list-fetch code, so the filter silently did nothing (found by the user, not by any validation in either feature). Every future phraseforge SPA-shell list view must read `new URLSearchParams(window.location.search).get("language")` and pass it to its own list API call — see the fix in texts-app.js/dialogs-app.js's showList().
2026-09-24T15:14:01Z [decision] phraseforge's multi-page navigation (pf-nav, the sidebar/header links) is no longer required to keep working with JavaScript disabled — the user said relying on JS is fine if it enables better architecture. pf-nav's own current doc comment cites the no-JS-fallback requirement as the reason it can't be a kb-nav-style data-driven tab/click component; that reasoning no longer holds as a hard constraint for future work (e.g. phraseforge-spa-unified-shell), though pf-nav itself is unchanged by this decision alone.
2026-09-24T16:11:22Z [gotcha] Setting el.style.display = '' to reveal an element only clears the inline override and falls back to the stylesheet cascade — if any class/type stylesheet rule (not just the UA [hidden] rule) sets display:none on that element with equal or higher specificity and no inline style currently wins, it stays hidden. layout.html's `.transcribe-action { display: none; }` (a class selector) silently defeated editor.js's `button.style.display = cfg.needs_transcription ? '' : 'none'` this way — the Transcribe button never showed for any language needing transcription, on both the tabbed (texts/dialogs) and tabless (vocabulary/models) editor paths, since day one. Fix: set an explicit non-empty value (e.g. 'inline-block') to reveal, never ''; only a non-empty inline style reliably overrides a same-importance stylesheet rule regardless of specificity. See phraseforge/internal/server/static/js/editor.js's pfRefreshActionVisibility and pfApplyImeConfig.
2026-09-24T16:29:40Z [gotcha] In phraseforge's unified SPA shell, all six sections' DOM subtrees coexist on one page (each hidden via style.display when inactive) — several sections' own forms reuse the same element ids (#language, #script, #field-source, #translateBtn, #translation-target), which editor.js and phraseforgeGenerate() (layout.html) look up via plain document.getElementById(). Visiting two sections' forms in one session leaves two elements sharing an id in the document; getElementById returns whichever is FIRST in document order, not necessarily the visible one. Fix: shell.js's showSection() clears the PREVIOUS section's container's innerHTML on every switch, so at most one section's markup exists in the DOM at a time — safe because every section already fully re-renders itself via root.innerHTML on its own show(). See phraseforge/internal/server/static/js/shell.js.
2026-09-24T16:29:41Z [convention] Superseded by phraseforge-spa-unified-shell: the 2026-09-24T13:43:34Z entry about reading window.location.search for the sidebar language filter no longer applies — there is only one URL now, no page reload carries a query string, and every section reads the filter from window.pfGetLanguageFilter() (shell.js) instead. Left for the next memory.md maintenance pass to purge (criterion 3, resolved transient) rather than edited in place.
2026-09-24T17:52:29Z [gotcha] phraseforge/k8s/jobs/migrate-job.yaml is a separate pod spec from phraseforge/k8s/deployment.yaml — when phraseforge/internal/config/config.go moved PGHost/PGPort/PGDatabase from env vars to the mounted config.yaml (only PGUSER/PGPASSWORD/SESSION_KEY/OLLAMA_API_KEY/OPENROUTER_API_KEY stayed env-only), the migrate Job manifest was not updated at the same time, so its container fell back to config.Load's Go default (localhost:5432) and the migration failed with connection refused. Any pod running phraseforge, not just the main Deployment, needs the phraseforge-config ConfigMap mounted plus CONFIG_FILE set.
